# DayDay 멀티 버티컬 SaaS 설계 지침 & 운영 표준 (Multi-Vertical SaaS Rules)

본 문서는 DayDay(튜링) 음악학원 MVP 개발을 시작으로 개인 레슨, 피트니스/헬스장, 소규모 F&B 매장 등 다양한 영세 사업자용 관리 솔루션(ERP)으로 확장해 나가기 위해 확정된 제품 철학, 데이터 설계 규칙, 상태 관리 및 코딩 컨벤션을 기록합니다.

---

## 1. 제품 철학 (Product Philosophy)

*   **진정한 소상공인용 올인원 ERP**: DayDay는 단순한 단방향 출결용 알림톡 앱에 그치지 않습니다. 영세 사업자의 **회원 관리, 스케줄 운영, 수납/결제, 그리고 최종적인 경영 정보(손익/지표)**를 포괄하는 종합 백오피스 솔루션을 지향합니다.
*   **1차 목표**: 음악학원 원장, 강사, 학부모가 즉시 사용할 수 있는 고기능 MVP 론칭.
*   **장기 확장 전략 (Vertical-Specific Instances)**:
    - 하나의 거대한 프로그램 안에 수많은 설정 버튼을 쑤셔 넣어 모든 업종을 커버하는 과도한 다기능(Generic) 앱을 만들지 않습니다. 이는 UX를 훼손하고 개발 난이도를 비약적으로 상승시킵니다.
    - **업종별 제품(Instance)을 완전히 분할하여 출시**합니다. 각 업종에 맞추어 화면 레이아웃, 표시 용어, 업무 프로세스 흐름을 딥(Deep)하게 밀착 설계합니다.
    - 대신, 백그라운드에서 동작하는 데이터 상태 엔진(Core State Engine)과 기초 데이터 아키텍처는 100% 동일한 공통 코어를 재사용하여 개발 생산성을 보존합니다.

---

## 2. 공통 도메인 영역 vs 업종별 차별화 요소

### 2.1 공통화할 수 있는 데이터 도메인
다음 영역은 비즈니스 형태가 달라도 SaaS 코어에서 범용적으로 설계하여 재사용합니다:
*   **조직/사업장 정보 (`organization`)**: 지점 정보, 가입 초대코드, 도장/서명, 로고 메타.
*   **사용자/계정/권한 (`authUsers`)**: 로그인 계정, 소셜 SNS 맵핑, 역할(원장/직원/고객)별 제한적 접근 권한.
*   **회원/고객 (`members`)**: 서비스 수혜자 인적 사항, 가족관계(보호자-피수혜자) 연동 매핑.
*   **직원 관리 (`staff`)**: 강사/트레이너/매니저 프로필, 일자별 근무 시프트.
*   **상품/서비스 (`catalog`)**: 수강 과목, 교재비, PT 회원권, 대여 락커 등의 마스터 요금표.
*   **일정/예약/세션 (`sessions`)**: 요일별/강사별 수업 스케줄, 대여 예약, 타임 슬롯 바인딩. 과거 및 오늘의 일정은 당시 스냅샷(이력) 기준으로 보존되고, 내일/미래 일정은 기본 스케줄 템플릿(원생 기본값)에서 동적으로 생성하는 재생성 규칙을 표준화합니다.
*   **출석/이용 기록 (`attendance`)**: 입장/퇴장 체크인 로그, 무단 결석 처리, 보강/취소 히스토리.
*   **결제/수납 (`billing`)**: 월별 청구서 발행, 토스/카카오페이 가상 결제, 영수증 승인.
*   **비용/손익**: 강사 비율제 정산 및 매장 소모품비 등의 간이 회계 데이터.
*   **공지/메시지/알림 (`communication`)**: 알림톡 발송 내역, 만족도/참석 설문조사 배포 및 응답 통계.
*   **설정 및 메타**: 시스템 알림 옵션 및 다국어/용어 치환 매핑 테이블.
*   **감사로그/변경 이력**: 결제 강제 변경 등 중요 작업 기록 추적.

### 2.2 업종별 특화 설계 영역 (차별화 포인트)
화면 표현과 비즈니스 룰은 다음 예시와 같이 각 업종에 완전히 몰입하여 다르게 구현합니다:

*   **음악학원 버전**:
    - *용어*: 원생, 학부모, 강사, 악기, 레슨, 교재, 수납 완료/미납.
    - *특화 기능*: 자녀를 학원에 보낸 보호자(학부모) 중심의 1:N 자녀 캘린더 연동, 등하원 안심 알림톡 발송, 교재 진도 체크 및 교재비 수강료 합산 청구.
*   **헬스장/피트니스 버전**:
    - *용어*: 회원, 트레이너, 회원권, PT 세션, 락커/운동복, 바코드 체크인, 입장 만료.
    - *특화 기능*: 게이트 바코드/지문 인식 입장 로그 결합, 만료 예정 회원 재등록 영업 상태 트래킹, 트레이너 매출 기여도 및 수업 비율제 정산(Salary).
*   **음식점/F&B 버전**:
    - *용어*: 테이블, 주문, 메뉴, 원가, 재고, 주방 주문, 직원 시프트.
    - *특화 기능*: 회원 개념보다는 실시간 주문 발주 및 테이블 점유율 현황, 주방 딜레이 타임 제어, 식자재 유통기한 및 마진율 추적.
*   **개인 레슨 버전**:
    - *용어*: 학생, 튜터, 수업료, 회차권, 보강, 개별 조율.
    - *특화 기능*: 매월 정기 납부가 아닌 4회/8회 단위 회차제 요금 차감, 보강 약속 자동 매칭 캘린더, 강사 1인 중심의 심플한 모바일 뷰어 중심 UI.

---

## 3. 공통 코드 규칙 및 아키텍처 제약사항

1.  **StateStore 직접 접근 금지 (`stateStore.db`)**:
    - 모든 UI 뷰 파일은 `stateStore.db`에 직접 접근하여 탐색할 수 없습니다. 
    - 반드시 `stateStore.getStudent(id)` 등 정의된 Public API 메소드만을 사용해야 합니다.
2.  **화면 로직과 데이터 상태 로직의 분리**:
    - 화면 렌더링을 담당하는 뷰 컴포넌트(`views/*.js`)에서는 UI 생성 및 이벤트 리스너 바인딩만 담당하며, 데이터를 직접 가공하거나 변경하는 연산은 모두 `stateStore` 내부로 위임합니다.
3.  **대용량 파일 분리 및 모듈 공유**:
    - `director.js`와 같이 크기가 비대해진 파일은 기능 컴포넌트별로 작게 분리하되, 분리된 하위 뷰 모듈들은 반드시 동일한 `stateStore` 싱글톤 인스턴스를 공유해야 합니다.
4.  **UI 문구의 하드코딩 배제**:
    - 화면 표기 단어는 추후 업종명 치환을 대비하여 뷰 내부에서 하드코딩하지 않고, 용어 사전 포맷터를 경유하도록 설계합니다.
5.  **공개 API 영향도 제어**:
    - 기존 Public API 명칭이나 반환 포맷을 변경할 때에는 뷰 레이어 전체에 미치는 파급 효과를 검증하고 문서를 선제 업데이트해야 합니다.

---

## 4. 데이터베이스(DB) 전환 설계 원칙

현재 로컬 프로토타입 상태에서는 브라우저 `localStorage`에 의존하나, 실제 론칭 단계(Supabase, PostgreSQL 등)를 대비한 원칙을 엄격히 고수합니다:

*   **완충 계층으로서의 `stateStore` 유지**:
    - 실제 서버 DB의 테이블 구조가 변경되더라도, 화면 레이어는 직접적인 타격을 받지 않게 `stateStore`가 API 번역 및 완충 역할을 지속해야 합니다.
*   **비동기 점진적 전환**:
    - 네트워크 호출로 인해 모든 조회 API가 비동기(`Promise`)로 바뀌게 됩니다. 
    - 이때 화면 컴포넌트 전체를 한 번에 `async/await`로 전환하는 폭탄형 개조는 삼가며, **초기 론칭 시 서버 스냅샷을 메모리 캐시에 밀어 넣은 뒤 동기 조회 API처럼 서빙**하고, 데이터 쓰기는 백그라운드 큐를 이용해 순차 비동기 업로드하는 완충 캐시 설계를 권장합니다.
*   **오프라인 대응(Offline-First)**:
    - 매장 현장의 네트워크 환경은 불안정할 수 있습니다. 로컬스토리지/IndexedDB 오프라인 캐시를 통해 인터넷 연결이 유실되더라도 등하원 출결 마킹과 일지 작성이 정상적으로 대기열에 기록되고, 네트워크가 재연결될 때 동기화가 일어나도록 설계합니다.

---

## 5. 직원 및 실무자 중심의 제품 설계 가이드

이 소프트웨어는 학원을 관리하는 '경영자(원장)'만을 위한 시스템이 아닙니다. 매장에서 매일 이 도구를 붙잡고 살아야 하는 '직원(강사, 매니저, 알바)'이 가장 편리하게 쓸 수 있어야 제품이 생존합니다.

*   **직원의 시야 - "오늘 내가 해야 할 일"**:
    - 직원은 복잡한 학원의 월별 총매출이나 손익 차트를 볼 필요도 없고 보고 싶어 하지도 않습니다.
    - 강사 화면에 진입하자마자 **"오늘 내가 가르칠 학생 목록"**, **"오늘 보내야 할 개별 안내장 수"**, **"조율 필요한 보강 건수"**만 직관적으로 보이게 집중 설계합니다.
*   **강사의 피로감 차단**:
    - 등하원 출결 입력과 수업일지 작성이 터치 2~3회 안에 종결되어야 수업 준비에 지장을 주지 않습니다. 이미지 업로드 시 모바일 전송 속도 및 저장 용량 한도를 줄이기 위해 모바일 브라우저 레벨에서 압축 전송합니다.
*   **행정 매니저의 사고 차단**:
    - 매니저가 가장 두려워하는 것은 결제 누락과 상담 예약 충돌입니다. 
    - 수납 만료일 도래 5일 전 대상자를 대시보드에 적색 경고로 띄우고, 학부모 알림톡 미수신 이력 등을 바로 캐치해 수동 조치를 유도합니다.
*   **직원 입력과 원장 지표의 연동**:
    - 직원이 일상적으로 처리하는 출결 마킹, 교재 승인 신청, 수납 처리가 백그라운드에서 안전하게 가공되어 원장 대시보드의 당월 수납률 및 연간 이탈률 등 핵심 경영 지표(KPI)로 은밀하고 자연스럽게 연계되게 처리합니다.
    - 직원 권한에서는 원장의 경영 정보에 무단 접근하거나 임의로 결제 완료 금액을 변경 및 삭제하지 못하도록 철저히 가드로 래핑합니다.

---

## 6. 출시 전 보안 원칙 (Security Principles)

*   **민감한 기밀 정보 코드 내 포함 금지**: GitHub token (`ghp_...`), API key, 고객 개인 정보, 실제 결제 수납 정보 등은 절대 소스코드, 문서, Git 커밋에 포함시키지 않습니다.
*   **유출된 토큰의 즉시 폐기**: `ghp_...` 형태의 GitHub personal access token이 단 한 번이라도 노출되거나 커밋 기록에 포함된 흔적이 보이면, 안전하지 않은 것으로 간주하고 즉시 GitHub 설정에서 폐기(Delete/Revoke) 처리합니다.
*   **데모용 기본값 및 하드코딩 패스워드 제거**: 시제품 단계에 적용된 데모용 기본 패스워드(`0000`) 및 키오스크 관리자 모드 해제용 하드코딩 패스워드(`6990`)는 운영 배포 전에 정식 설정이나 사용자 인증 구조로 마이그레이션하여 완전히 제거해야 합니다.
*   **평문 비밀번호 저장 금지**: 사용자 패스워드나 PIN은 데이터베이스(DB) 내부에 어떠한 경우에도 평문(Plaintext)으로 저장해서는 안 되며, 단방향 안전 해시 알고리즘(bcrypt 등)을 적용해 보존합니다.
*   **localStorage 데이터 저장 통제**: 브라우저 `localStorage`에는 실제 고객 개인 정보나 결제 관련 중요 정보는 저장하지 않아야 하며, 향후 서버 DB 연동 시 이들을 안전한 서버 사이드 데이터 모델로 이관합니다.
*   **실질적인 역할/권한 검증 구현**: 화면 분리(라우터 가드) 수준에 그치지 않고, 서버 DB 전환 시 백엔드 API 레벨에서 행 수준 보안(RLS, Row Level Security) 정책 및 JWT 권한 토큰 검사를 거쳐 원장, 매니저, 강사, 학부모 등 역할별 정보 격리를 실현합니다.
*   **백업/임시 파일 커밋 예방**: 작업 도중 발생하는 `.backup`, `.bak`, `.old` 등 소스 복사본 파일은 깃 원격 저장소에 업로드되지 않도록 직접 파기하고, 깃 추적 설정에서 배제시킵니다.
*   **멀티 테넌트 격리 및 업종 확장 설계**: 데이터베이스(DB) 레벨에서는 다중 업종 확장을 고려하여 최상위 격리 단위를 `organizations`로 일관성 있게 구성하고, 프론트엔드/화면 UI 용어사전을 매칭하여 음악학원 버전에서는 '학원(academy)', 헬스장 버전에서는 '센터(center)' 등으로 유연하게 노출합니다.
*   **어댑터(Adapter) 기반 저장소 전환**: DB 및 로컬 파일 등 데이터 저장소의 변경 과정에서 화면(View) 렌더링 코드를 비동기(`async/await`)로 대량 전환하는 충격을 예방하기 위해, `StateStore`와 데이터 레이어 사이에 인터페이스를 두어 완충하는 데이터 어댑터 구조를 의무화합니다.
- **저장소 구현체 은닉**: 뷰(View) 레이어는 로컬스토리지, Supabase, 또는 향후 도입될 어떤 데이터 저장소 구현체든 직접 알지 못하게 통제하며, 오직 `StateStore` Public API에만 의존합니다.
- **API 호환성 우선**: 저장소 분리/마이그레이션 도중에도 뷰의 작동이 멈추지 않도록 Public API 명칭과 동기 캐시 조회 호환성을 우선 보장합니다.
- **테넌트 격리 키 규칙**: 모든 DB 어댑터의 내부 테넌트 격리 키는 `organizationId`를 표준으로 삼고, UI 및 레거시 데이터 매핑 영역에서 호환성을 위해 `academyId`를 동치(Alias)로 취급하여 처리합니다.
- **클라이언트 Role 비신뢰 원칙**: 프론트엔드가 송신하는 사용자 역할 정보(`role`)를 절대 맹신하지 않으며, 쓰기/읽기 권한 검증은 오직 DB 레벨 RLS(`organization_members` 매핑 검증) 또는 서버 API 토큰 디코딩 값만을 기준으로 수행합니다.
- **감사 로그 대상 규정**: 결제 승인 상태의 강제 변경, 개인 정보의 수정/삭제, 출결 강제 마킹 등 중요 비즈니스 엔티티 트랜잭션 수행 시에는 반드시 `audit_logs` 테이블에 연계 변경 기록을 강제 누적하도록 아키텍처를 공유합니다.
- **구조 공통성 유지**: 다른 업종용 버전을 생성할 때 화면의 표기 단어, 레이아웃 및 폼 양식은 개별 커스터마이징을 수행하되, 백오피스 권한 제어 모델(RLS, 테넌트 격리, 감사 로그, SDK 분리)은 완전히 공통화된 Core 엔진 설계를 공유하여 배포 일관성을 사수합니다.
- **`service_role` 브라우저 유출 엄금**: 어떠한 경우에도 브라우저 클라이언트 사이드 코드 및 환경 설정에 Supabase `service_role` 키를 포함시키지 않습니다.
- **정본 작업 폴더 준수 (정본 폴더 원칙)**: 모든 코드 수정, 검증 커맨드 실행, 테스트 및 커밋은 반드시 정본 프로젝트 폴더(`C:\Users\charm\OneDrive\문서\2605_dayday`) 내에서만 수행합니다. 과거의 카피/검토용 폴더(`C:\Users\charm\Dropbox\100. LLM WIKI\2605_dayday-codex-copy`)는 어떠한 경우에도 읽기/수정/테스트/커밋 등 작업 대상으로 사용하지 않으며 엄격히 배제합니다.
- **다중 배포 환경(Preview/Production) 분리**: 상용 서비스 안정성을 위해, Pull Request 기반 고유 Preview 배포 환경과 Main merge 기반 Production 실환경을 엄격히 구분하고, 이에 주입되는 Supabase 및 자격 증명 식별값을 Sandbox/Real 용도로 상호 이중화 격리 적용합니다.
- **배포 전 필수 수동 시나리오 검증**: 코드 배포 전 `node --check` 및 `smoke_test.mjs` 검사와 더불어, 3대 도메인 주체(원장, 강사, 학부모/Kiosk)의 핵심 업무 시나리오(조회, 수정, 출결 마킹, 수납)를 브라우저에서 직접 수동 검수하는 것을 배포 프로세스에 포함시킵니다.
- **Git 커밋 전 민감정보 자동 스캔**: 모든 신규 코드의 커밋 및 푸시 전, PowerShell 스크립트 기반 기밀 유출 검사를 생활화하여 비밀 자격 증명이 실수로 원격지에 업로드되는 것을 원천 차단합니다.
- **배포 베이스라인 고정 원칙**: 외부 모듈(SDK 등) 또는 실서버 비동기 데이터 통신을 연동하기 전, 뷰와 어댑터 레이어 간의 동기 Mock 인터페이스 정합성을 테스트용 스크립트로 100% 검증하여 릴리즈 기준선(Baseline)을 확정한 뒤 Git 스테이징 및 커밋 준비 상태를 선언합니다.
- **E2E 네비게이션 안정화 규칙 (E2E 안정화 원칙)**:
  - `page.goto`는 `domcontentloaded` 기준으로 이동 처리합니다 (`{ waitUntil: 'domcontentloaded' }`).
  - 로그인 전 `.role-grid` 또는 역할 선택 UI가 `attached`/`visible` 상태가 될 때까지 명시적으로 대기합니다.
  - 역할 버튼 클릭 후 `#login-overlay`가 `hidden`이 될 때까지 대기합니다.
  - `#app-root`가 `visible`이 될 때까지 대기합니다.
  - 메뉴 클릭 전 대상 요소가 `attached`/`visible` 상태인지 확인합니다.
  - 오버레이나 페이드 트랜지션 때문에 클릭이 가로막히는 경우, 먼저 `scrollIntoViewIfNeeded()`를 수행하고 필요 시 `evaluate(el => el.click())` 방식으로 클릭합니다.
  - 한글 텍스트 기반 선택자보다 `data-testid`, `data-view`, `id`를 우선 사용합니다.
  - 테스트는 기능 검증에 필요한 최소한의 DOM 계약만 검증하고, CSS 세부 스타일에는 과도하게 의존하지 않습니다.

*   **작은 Phase/Repair Phase 원칙**:
    - 큰 작업을 한 번에 수행하지 않고, 기능이 꼬이거나 미해결된 경우 Repair-A, Repair-B처럼 작은 작업 단위로 쪼개어 해결합니다.
    - 다음 개발 작업은 반드시 `Phase 7F-Repair-A: 출력 버튼 실제 동작 복구`부터 시작합니다.

*   **완료 판단 원칙**:
    - 자동 테스트(`npm run test:full` 또는 Playwright E2E)의 통과 결과만으로 개발 완료를 판단하지 않습니다.
    - 사용자가 실제 브라우저에서 클릭하여 확인해야 하는 기능은 완료 보고 후 반드시 "사용자 확인 필요"로 명기합니다.
    - 특히 프린트 모달, 이미지 다운로드/복사, 드래그앤드롭, 검색 UX 기능은 수동 검증을 거쳐야 최종 완료로 간주합니다.

*   **StateStore Public API 원칙 & 외부 연동 제한**:
    - 뷰(View) 파일에서 `stateStore.db` 내부 속성에 직접 접근하는 것을 절대 금지하며, 반드시 공개된 Public API를 사용합니다.
    - 실제 Supabase/Firebase 서버 DB 연동, 결제/알림톡/OAuth 및 외부 이미지 변환 API 연동은 전면 제한되며, 현재는 `LocalStorageAdapter` 기반 MVP 흐름을 깨지 않고 유지합니다.

*   **Git 원칙 (push 보류 원칙)**:
    - 작은 단위마다 로컬 커밋은 가능하나, 원격 저장소로의 `git push`는 사용자의 명시적인 요청이 있을 때에만 수행합니다.
    - `git reset`, `checkout` 등 destructive command를 사용자를 통한 확인 없이 임의로 수행하는 것을 엄격히 금지합니다.

*   **강사 시간표 및 출근표 관리 UI/UX 원칙**:
    - **통합 운영 환경**: '강사 출근표 관리'와 '강사-원생 시간표 관리'는 동일한 시스템의 운영 도구로서 UI/UX 일관성을 사수합니다.
    - **출력 기능**: 출력하기 버튼은 화면 우측 상단에 단 하나만 일관되게 제공하며, 중복 노출된 다른 출력 버튼은 제거합니다. 버튼 클릭 시 프린트 모달에서 1개(가로 권장)/2개/3개(세로 권장) 배치를 선택해 인쇄할 수 있어야 합니다.
    - **원생 특이사항 패널**: 오른쪽 고정 패널 구조로 영역을 통일하며, 포맷은 첫 줄에 `[원생명] [담당강사명]`, 두 번째 줄에 `[특이사항 텍스트]` 구조로 배치합니다.
    - **참여 강사 기본 필터**: 일간 보기에서는 기본적으로 선택된 날짜에 수업/출근이 있는 참여 강사만 노출합니다. 기존에 제공되던 `당일 수업 강사만` 토글 버튼은 제거하고 이 동작을 기본 규칙으로 내재화합니다.
    - **강사 검색 UX**: 일간 보기 강사명 검색은 `input + dropdown` 또는 `datalist` 구조로 구현하며, 입력 중 리렌더링으로 인해 input 요소가 교체되거나 포커스가 유실(blur)되지 않아야 합니다.

*   **날짜별 시간표/운영표 기록 및 조회 규칙**:
    - **과거 및 오늘 날짜**: 당시 날짜에 기록되어 영속화된 운영표 이력(스냅샷)을 기준으로 표시해야 합니다. 만약 원장이나 강사가 특정 원생의 시간표를 임의로 이동/수정했다면, 해당 날짜의 운영표는 이동된 고정 상태(Override)로 유지되어야 합니다.
    - **미래 날짜 및 다음 주차 시간표**: 기본적으로 원생 프로필에 등록된 '기본 시간표(default classes)' 설정을 템플릿으로 삼아 매번 동적으로 렌더링해야 합니다. 미래 시점의 기본값을 바꾸려면 원생 정보의 기본 시간표 자체를 수정해야 합니다.
    - **Phase 7C 범위 (완료)**: 일간/주간 시간표의 읽기, 표시, 날짜 및 강사 필터, 특이사항 패널 및 토글 상태를 구현하고, 날짜별 Override 스케줄 조회(getTeacherStudentScheduleForDate)를 매핑 완료하였습니다.
    - **Phase 7D-1.5 범위 (완료)**: 날짜별 시간표/운영표의 과거/오늘/미래 데이터 처리 규칙의 정합성을 검증하고, unit 테스트(schedule_override_test.mjs)와 package.json 파이프라인 연계 및 Public API/normalization 예방 로직을 보강하였습니다.
    - **Phase 7D-2 범위 (완료)**: 사용자가 시간표 슬롯 내에서 원생 수업 카드를 드래그하여 이동하거나, 날짜별로 변경 사항(Override)을 데이터베이스에 저장(moveStudentScheduleForDate)하고 로깅하는 영속화 기능 및 UI를 연동하고, Playwright E2E 테스트로 검증을 완료하였습니다.
    - **Phase 7D-2.5 범위 (완료)**: E2E 전용 드롭 트리거(__triggerDrop)를 E2E 플래그(window.__DAYDAY_E2E__) 조건부 하에 안전 격리하고 Phase 명칭 및 문서 정합성을 일괄 정돈 완료하였습니다.
    - **Phase 7D-3 범위 (완료)**: 날짜별 일정 이동 이력에 대한 감사 로그 조회 및 시간표 변경 이력(Operation Logs) 패널 UI와 토글 기능 연동을 보강하고 Playwright E2E 테스트로 검증을 완료하였습니다.
    - **Phase 7E 범위 (완료)**: 강사 출근표(shifts) 및 강사-원생 시간표(matches) 내에 '출력하기' 버튼을 추가하고, A4 규격의 모던 프린트 미리보기 팝업 모달 구현, 인쇄 스타일 시트(@media print) 바인딩, PNG 이미지 다운로드 및 클립보드 이미지 복사 기능(캡처 상태 로딩 인디케이터 및 날짜별 파일명 동적 적용) 고도화, 그리고 Playwright E2E 테스트를 통한 출력/이미지 내보내기 흐름 검증을 100% 완료하였습니다.
    - **Phase 7F 범위 (완료)**: 인쇄/이미지 내보내기 미리보기 시 화면 가로/세로 레이아웃(주간 뷰는 가로, 일간 뷰는 표시 컬럼 수 기준)에 따른 자동 용지 방향 매핑 및 동적 가이드 팁을 화면에 적용하고 E2E 테스트로 검증을 마쳤습니다.

---

## 7. 멀티 버티컬 공통 테스트 및 품질 보증 규칙 (Multi-Vertical Testing Rules)

향후 음악학원을 포함한 모든 업종 버전(피트니스, 레슨 등)의 DayDay 확장판을 개발할 때, 전체 품질과 보안 정합성을 보장하기 위해 아래 테스트 규칙을 의무적으로 준수해야 합니다:

1.  **기능 추가 전후 `test:full` 의무 실행**:
    *   새로운 피처 구현, 레이아웃 변경, 상태 속성 리팩토링 전후에는 반드시 로컬 터미널에서 `npm run test:full`을 구동하여 상태 검사, 모크 어댑터 데이터 정합성, 기밀 스캔 및 실제 브라우저 E2E 시나리오 검사를 100% 통과해야 합니다.
2.  **역할군(Role)별 핵심 유저 플로우 수동/자동 검증**:
    *   **원장/관리자**: 대시보드 지표 확인, 요금 상품/수강 일정 설정 등록, 프로필 서명 업데이트 기능 점검.
    *   **직원/강사**: 오늘 수업 목록 조회, 근무 시프트 배정 상태 및 출결 강제 마킹 권한 확인.
    *   **고객/학부모/Kiosk**: PIN 코드 출결 터치 시나리오, 실시간 상태 전달 및 가상 결제 승인 확인.
3.  **최우선 테스트 대상 도메인**:
    *   비즈니스 무결성에 치명적인 영향을 주는 **수납/청구(Billing)**, **출결/입장(Attendance)**, 그리고 **고객 개인정보 격리** 기능은 마이너 수정 시에도 우선적으로 정합성 테스트를 수행해야 합니다.
4.  **실제 외부 리소스 연결 전 Mock 테스트 필수화**:
    *   실제 Supabase DB 호스트, 토스페이먼츠 실결제 게이트웨이, 카카오 알림톡 API와 연결하기 전, 내부 `Mock Client` 및 `adapter` 규격 하에서 데이터 정합성이 완전하게 돌아가는지 선 검증해야 합니다.
5.  **배포 전 브라우저 콘솔 에러 검증**:
    *   실 서버 배포 직전 브라우저 개발자 도구의 Console을 열고 로그인, 대시보드 로딩, 탭 전환을 거치며 `Unexpected token`이나 `ReferenceError` 등 붉은색 콘솔 에러가 단 1건도 표시되지 않는지 최종 수동 또는 E2E 자동 점검해야 합니다.


