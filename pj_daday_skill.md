# DayDay Multi-Vertical SaaS Architecture & Framework Guidelines

본 문서는 DayDay(튜링) 음악학원 관리 시스템을 MVP로 완성하고, 향후 개인 레슨, 피트니스/헬스장, 음식점 등 다양한 업종별 맞춤형 SaaS 서비스로 신속하게 수평 확장(Horizontal Scaling)하기 위한 공통 규칙, 데이터 모델 설계 기준, 네이밍 컨벤션, 설계 의사결정 사항 및 체크리스트를 기술합니다.

---

## 1. 다중 업종 확장을 위한 데이터 모델 설계

각 업종별로 다루는 엔티티의 도메인 성격은 유사하나 용어와 세부 필드가 다릅니다. 다중 업종 확장을 지원하기 위해 핵심 테이블 구조를 추상화하여 다음과 같이 맵핑 규칙을 정의합니다.

### 1.1 도메인 개념 맵핑 테이블

| 핵심 역할 | 공통 추상 클래스 / 테이블 | 음악학원 버전 (MVP) | 개인레슨 버전 | 헬스장/피트니스 버전 | 음식점/F&B 버전 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **서비스 대상** | `Client` | `students` (원생) | `tutees` (학생) | `members` (회원) | `reservations` / `tables` (예약/테이블) |
| **서비스 공급자** | `Staff` | `teachers` (강사) | `tutors` (교사) | `trainers` (트레이너) | `staff` / `chefs` (직원/요리사) |
| **시간표/일정** | `Schedule` | `classes` (수업 일정) | `lessons` (레슨 일정) | `sessions` (수강 일정) | `bookings` (시간대 예약) |
| **매출/결제** | `Billing` | `payments` (수납/청구) | `payments` (수업료 청구) | `memberships` (이용권/결제) | `orders` (주문/영수증) |
| **출결/기록** | `Log` | `attendance` (출결 로그) | `logs` (수업 일지) | `checkins` (입장 로그) | `visits` (방문 이력) |
| **비즈니스 메타** | `AcademyInfo` | `settings` (학원 정보) | `settings` (레슨 정보) | `settings` (센터 정보) | `settings` (매장 정보) |

### 1.2 고유 식별자(ID) 접두사 컨벤션
다중 테이블 조인 및 식별의 용이함을 위해 고유 키(Primary Key)에 문자 접두사를 부여하는 규칙을 전 업종 공통으로 유지합니다.
*   **고객 엔티티 (`Client`)**: `S` (Student/Subject) -> 예: `S1`, `S2`
*   **공급자 엔티티 (`Staff`)**: `T` (Teacher/Trainer/Tutor) -> 예: `T1`, `T2`
*   **스케줄 엔티티 (`Schedule`)**: `C` (Class/Course/Calendar) -> 예: `C1`, `C2`
*   **수납 엔티티 (`Billing`)**: `P` (Payment/Purchase) -> 예: `P1`, `P2`
*   **출결 엔티티 (`Log`)**: `A` (Attendance) -> 예: `A1`, `A2`

---

## 2. 상태 관리 및 성능 최적화 규칙

반바닐라 JS 환경에서 안정적인 상태 관리와 저장소 동기화를 위해 다음 규칙을 준수합니다.

### 2.1 단방향 상태 동기화 및 Pub/Sub 패턴
*   모든 데이터 쓰기 및 수정 작업은 `stateStore` ([state.js](file:///c:/Users/charm/OneDrive/문서/2605_dayday/src/js/state.js)) 클래스 내부에서만 수행합니다.
*   데이터 변경이 완료되면 반드시 `saveDB()`로 로컬 저장소를 갱신하고, 해당 엔티티의 변경 이벤트(예: `STUDENTS_CHANGED`)를 `notify()`로 방출하여 화면단이 스스로 갱신되게 설계합니다.
*   컴포넌트 소멸 시 메모리 누수를 방지하기 위해 `subscribe` 등록 후 리턴받은 `unsubscribe` 함수를 반드시 캐싱하여 모달이 닫히거나 페이지가 바뀔 때 소멸 처리합니다.

### 2.2 대량 데이터 처리를 위한 일괄 등록(Batch) API
*   엑셀 업로드 등 수십~수백 건의 데이터가 동시 유입될 때 루프 내에서 개별 `saveDB()`와 `notify()`를 호출하는 것은 브라우저 메인 스레드를 차단하고 UI 버벅임을 유발합니다.
*   대량 등록 프로세스는 반드시 **일괄 처리 API (`addStudentsBatch`)**를 설계하여 아래 로직을 따릅니다:
    1. 메모리상에서 순번 및 ID 채번을 일괄 처리하여 배열에 푸시
    2. 로컬스토리지 저장(`saveDB()`) 1회 수행
    3. UI 변경 이벤트 통지(`notify()`) 1회 수행
*   이 방식은 디스크 I/O 병목을 제거하여 성능을 약 10배 이상 향상시킵니다.

### 2.3 LocalStorage 한도 관리 및 이미지 압축 정책
*   브라우저의 `LocalStorage` 용량 한도는 약 5MB 내외입니다.
*   학원장 서명 이미지, 직인 등의 이미지 업로드 시 base64 원본 데이터를 그대로 저장하면 저장소 용량 초과(`QuotaExceededError`)가 즉시 발생하여 시스템 전체가 마비됩니다.
*   **이미지 용량 통제 규칙**:
    *   모든 업로드 이미지(서명, 로고 등)는 업로드 즉시 Canvas API를 활용해 최대 150px 내외로 리사이징 및 압축(JPEG/PNG 데이터 URL 변환) 처리한 후 저장소에 주입합니다.
    *   데이터베이스 저장 구문(`saveDB()`) 및 일괄 등록 로직은 항상 `try-catch`로 감싸 예외가 발생할 경우 사용자에게 명확히 피드백(용량 한도 경고 등)을 제공합니다.

---

## 3. UI/UX 레이아웃 설계 규칙

다양한 화면 환경에서 시제품 수준의 완성도 높은 디자인 시스템(Premium Aesthetics)을 유지하기 위해 다음 가이드를 따릅니다.

### 3.1 모달 뷰포트 고정 및 내부 스크롤 규칙
*   모달이 화면을 벗어나거나 브라우저 전체 스크롤과 모달 스크롤이 이중으로 발생하는 문제를 방지합니다.
*   모달 영역에 `.layout-fixed` 클래스를 사용하고, 헤더와 푸터 영역을 화면 위아래에 고정(Sticky/Flex)한 뒤 실제 입력 폼 영역인 `.modal-form-scroll-body`에 `max-height`와 `overflow-y: auto;`를 주어 내부에서만 깔끔하게 스크롤이 일어나도록 통제합니다.

### 3.2 고정 테이블 헤더(Sticky Header) 비침 제거
*   테이블 스크롤 시 데이터 행이 고정 제목행(Header) 뒤쪽으로 겹쳐서 움직이는 투명화 현상을 원천 방지해야 합니다.
*   제목행(`th`)에 `position: sticky; top: 0; z-index: 10;`을 적용하고, 배경색은 **반투명(rgba)이 아닌 완전한 불투명(opaque) 배경색**을 명시합니다.
    *   *예시*: 흰색 테마 배경의 15% 파란색 반투명 효과를 내려면 `rgba(9, 132, 227, 0.15)`를 사용하지 않고 합성 색상인 `#daedfb`를 명시합니다.

### 3.3 스마트 연락처 입력 UX
*   고객 또는 직원 연락처의 기본값이 "없음"으로 설정되어 있어도 사용자가 입력창에 마우스를 클릭하고 숫자를 타이핑하기 시작하면, 드롭다운이 자동으로 "직접입력"으로 전환되고 정상 입력되도록 이벤트를 바인딩합니다.
*   비활성화(`disabled = true`) 처리를 피하고, '없음' 상태에서는 인풋 값을 비우되 상시 입력 가능한 상태로 둡니다.

---

## 4. 새 업종 인스턴스 론칭 시 체크리스트 (Vertical Checklist)

DayDay 프레임워크를 기반으로 새로운 업종 버전을 출시할 때 개발자가 확인해야 할 체크리스트입니다.

- [ ] **1단계: 데이터 스키마 정의**
  - [ ] 도메인 용어에 맞게 `state.js` 데이터 구조를 정의했는가? (원생 -> 회원 등)
  - [ ] ID 채번 접두사(`S`, `T`, `C`, `P` 등)가 도메인 설계 규칙을 따르고 있는가?
- [ ] **2단계: 설정 관리 및 저장소 용량 보존 검증**
  - [ ] 새 업종 설정 화면에서 파일 업로드 시 Canvas 압축 로직(150px 미만)이 포함되었는가?
  - [ ] 설정 저장 시 `QuotaExceededError`를 포착하여 알림창을 띄우는가?
- [ ] **3단계: 화면 컴포넌트 렌더링 최적화**
  - [ ] 스크롤이 발생하는 모든 모달에 `.modal-form-scroll-body` 레이아웃 고정이 적용되었는가?
  - [ ] 리스트 테이블의 sticky 제목행에 불투명(Hex) 배경색이 적용되었는가?
- [ ] **4단계: 성능 병목 분석**
  - [ ] 대량의 목 데이터 업로드 기능(CSV/Excel) 사용 시 루프 쿼리가 아닌 `addXXXBatch` 메소드를 활용하고 있는가?
  - [ ] 일괄 삽입 중 에러 처리 바운더리가 설정되어 있는가?

---

## 5. 수동 검증 가이드 (Manual Verification Guideline)

배포 전 필수 모듈이 올바르게 작동하는지 확인하기 위한 시나리오 리스트입니다.

### 5.1 엑셀/대량 업로드 기능 검증
1.  **동작**: 엑셀 서식을 다운로드한 후, 10건 이상의 데이터(일부 공란 및 일부 중복 데이터 포함)를 업로드합니다.
2.  **체크포인트**:
    *   미리보기 화면에서 마우스 휠 스크롤 다운 시 데이터 글자가 고정 제목행 뒤로 비쳐 보이지 않는가?
    *   제목행 배경색이 깨지거나 빈 틈이 없는가?
    *   '최종 등록 완료' 버튼 클릭 시 모달이 즉시 닫히고 우측 하단 카카오톡 레이아웃 형태의 토스트 알림창이 정상 노출되는가?
    *   원생 목록 테이블에 데이터가 정상 반영되는가?

### 5.2 연락처 스마트 입력 기능 검증
1.  **동작**: 원생 등록 또는 수정 모달을 엽니다. 본인/학부모 연락처 드롭다운을 '없음'으로 선택합니다.
2.  **체크포인트**:
    *   드롭다운이 '없음'으로 바뀌면 입력창의 텍스트가 깨끗이 비워지는가?
    *   입력창이 회색으로 비활성화(disabled)되지 않고 여전히 포커스 및 타이핑이 가능한가?
    *   입력창을 클릭하고 숫자 키(예: `0`)를 누르는 즉시 드롭다운이 '직접입력'으로 변경되며 숫자가 입력되는가?
